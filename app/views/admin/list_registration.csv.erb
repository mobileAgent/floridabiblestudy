"last name","first name","email","phone","mobile","addr","city","state","zip","balance","regdate"
<%@registrations.each do |reg| %><%="\"#{reg.last_name}\",\"#{reg.first_name}\",\"#{reg.user.email}\",\"#{reg.phone}\",\"#{reg.mobile}\",\"#{reg.address1} #{reg.address2}\",\"#{reg.city}\",\"#{reg.state}\",\"#{reg.zip_code}\",\"#{number_to_currency(reg.amount_owed, :precision => 2)}\",\"#{reg.created_at.iso8601}\"\r\n"%><%  end %>
